# This is the followee page who is BEING FOLLOWED by others
# People who the user is following
<h1><%= @user.first_name %>'s Followings</h1>

<ul>
  <% @user.followings.each do |following| %>
    <li><a href="/users/<%= following.id %>"><%= following.username %></a></li>
  <% end %>
</ul>
